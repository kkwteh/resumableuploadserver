name: OldUploadApp
options:
  bundleIdPrefix: com.navigateai
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
targets:
  OldUploadApp:
    type: application
    platform: iOS
    sources:
      - OldUploadApp
    settings:
      base:
        INFOPLIST_FILE: OldUploadApp/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        PRODUCT_BUNDLE_IDENTIFIER: com.navigateai.oldupload.app
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1,2"
